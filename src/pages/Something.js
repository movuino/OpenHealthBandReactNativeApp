import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default class Devices extends React.Component {
  render() {
    return (
      <View style={{ flex: 1, backgroundColor: 'green' }}>
        <Text> We are somewhere here </Text>
      </View>
    );
  }
}